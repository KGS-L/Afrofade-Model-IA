---
epic: "12-17"
track: marketplace
date: 2026-08-22
verdict: rejected
criteria: declared
headless: false
---

# Rétrospective — Piste Marketplace V2 (Épiques 12 → 17)

## Résumé de l'épique / piste

- **Piste :** Marketplace V2 — Épiques 12 (identité/professionnels/salons/entitlements), 13 (taxonomie/services/portfolio/découverte locale), 14 (disponibilités/réservations/Visual Brief/notifications), 15 (confiance/avis vérifiés/modération), 16 (carrières/recrutement), 17 (télémétrie/growth/préparation paiement).
- **Intervalle de diff / commits :** `d0c738e^..fad7166` (HEAD au 2026-08-22). 172 commits dont 3 merges (1 mesuré sur la colonne first-parent, 2 non mesurés — voir Limites).
- **Statut tracker :** `merged-and-validated` ; gates release M1–M5 `passed`. Les 39 stories (12.1 → 17.5) sont en statut `review` / `review_feature_gated` — **aucune en `done`**. L'utilisateur a explicitement accepté de procéder à la rétro sur ces stories non promues (décision humaine enregistrée ici).
- **Fusion :** PR #15 (`agent/bmad-ui-ux-future-marketplace`) fusionnée via `3e189e0` après réconciliation three-way avec main avancé de 13 commits (`1906fd4`, `606c31a`). Stories 17.4/17.5 complétées post-fusion directement sur main (`0120306`).

## Inventaire des preuves

| Élément | Source | État |
| :--- | :--- | :--- |
| Spec épiques + AC déclarées (39 stories) + DoD cross-story (10 points) | `_bmad-output/planning-artifacts/epics-marketplace-2026-08-21.md` (+ amendements UX V2 `epics-marketplace-ux-v2-amendments-2026-08-21.md`) | ✅ disponible |
| PRD marketplace | `_bmad-output/planning-artifacts/prds/prd-Afrofade-2026-08-21-marketplace/prd.md` | ✅ (modifications non commitées dans le working tree) |
| Architecture marketplace | `_bmad-output/planning-artifacts/architecture/ARCHITECTURE-AFROFADE-MARKETPLACE-2026-08-21.md` | ✅ |
| UX V2 validée | `_bmad-output/planning-artifacts/ux-designs/UX-AFROFADE-MARKETPLACE-V2-VALIDATED-2026-08-21.md` | ✅ |
| Fichiers stories BMAD | `story-12-1` → `story-12-7`, `story-13-1` → `story-13-7` (+ `spec-12-1` → `spec-12-6`) | ⚠️ partiel — **aucun fichier story pour les épiques 14–17** ; frontmatter statuses obsolètes (13 × `in-dev`, 1 × `ready-for-dev`) |
| Preuves git | `git_evidence.py` sur `d0c738e^..fad7166` → `/tmp/mkt-git-evidence.json` | ✅ 172 commits, 163 fichiers, +11 940 / −2 888 lignes (hors restatements de merge) |
| Attribution story→commit | idem | ⚠️ **16/39 stories seulement** ont des commits qui les nomment ; épiques 14–16 et 17.1–17.3 livrées via commits agrégats sans référence de story (ex. `5ac0488 feat(marketplace): complete Epics 13-17...`) ; 141 commits non-merge sans correspondance de story |
| Revue cross-epic | `MARKETPLACE-V2-CROSS-EPIC-REVIEW-2026-08-21.md` | ✅ fonctionnel présent ; **aucun run CI GitHub observable au moment de la revue** (gates passées ensuite suite à validation de fusion main) |
| Assets de validation | `scripts/check_marketplace_v2_contract.py`, `.github/workflows/marketplace-v2-review.yml`, contrats PostgreSQL `web/supabase/tests/` | ✅ présents (exécution à vérifier en Phase 2) |
| Sprint status | `marketplace-sprint-status.yaml` | ✅ (format de clés `N.M-` non reconnu par `detect-epic` → gate scriptée indisponible, liste établie manuellement) |
| Rétrospective précédente | `epic-5-retro-2026-08-18.md` (verdict `accepted`) | ✅ suivi des actions en Phase 4 |
| Logs de session | — | ❌ absents — analyse des leçons de processus limitée aux artefacts et commits |

### Limites d'attribution connues

- Deux commits 3D (`443e429` Epic 7, `3f25cf9` Épiques 8–11) sont entrelacés dans la fenêtre de la piste ; ils ne sont pas comptés dans les vues agrégées marketplace.
- `merges_measured: 1` / `merge_count: 3` : deux merges de la plage ne sont pas restatés dans `merge_files` (fusion de branche hors colonne first-parent).

## Constats (Phase 2)

Analyses exécutées : 3 lens de review (`adversarial`, `edge-case-hunter`, `verification-gap`) sur le diff `d0c738e^..fad7166` + 2 vues agrégées (`spec-to-implementation`, `duplication/pattern-divergence`), déléguées à 5 sous-agents parallèles, puis consolidées et recoupées. Vue **architecture delta** rétrécie : pas d'outil de graphe de dépendances natif ; la structure modules/imports a été dérivée via la vue duplication. Vue **god-class** : aucun (fichiers à fort churn ≤ 227 lignes au final) ; le churn se concentre dans les migrations SQL (18/33 fichiers).

### F1 — Fork de schéma 04/20 : la chaîne de migrations casse sur base vierge ⚠️ critique — **confirmé en exécution**

`04_role_dashboards.sql` (migration déjà livrée avant la piste) a été **modifiée en place** (+458 lignes, commits `a550fd0`, `5ac0488`) pour embarquer un schéma marketplace parallèle : `professional_profiles`, `salon_memberships`, `notification_outbox`, `job_postings`, `job_applications` y sont définies **deux fois** — dans le 04 et dans les migrations 20/41/60 — avec des vocabulaires de colonnes incompatibles (`full_name`/`verification_state`/`state` vs `professional_name`/`verification_status`/`status` ; CHECK `work_mode` divergents `04:615` vs `60:12`).

- Sources : `web/supabase/migrations/04_role_dashboards.sql:328-360,473-635` vs `20_...sql:11-129`, `41_...sql:4-19`, `60_...sql:4-59` ; corroboré par 4 agents indépendants.
- **Preuve comportementale (2026-08-22)** : application de la chaîne complète sur base vierge `postgres:16` (mock `auth`+`storage` comme les tests CI) → 01–12 OK, **échec à `20_marketplace_identity_foundation.sql:45` : `column "slug" does not exist`** — le `CREATE TABLE IF NOT EXISTS` de 20 ne fait rien (04 a déjà créé la table sans `slug`), puis son index unique sur `LOWER(slug)` échoue. **Aucun environnement neuf ne peut migrer au-delà de 20.**
- Environnements déjà déployés : les lignes ajoutées au 04 ne s'exécutent jamais → tables/functions du 04 absentes → les routes qui les interrogent (`booking-engine.ts`, `careers.ts`, `services-discovery.ts`) renvoient 500.
- Aucun test/CI n'applique la chaîne complète (les tests 20–25 construisent chacun leur mini-schéma) — c'est pourquoi c'est passé inaperçu.

### F2 — Backfill CROSS JOIN dans 04 : escalade de privilèges de masse ⚠️ critique

`04_role_dashboards.sql:444-466` : `backfill_legacy_salon_memberships()` fait `INSERT ... SELECT FROM user_profiles up CROSS JOIN salons s WHERE up.role='salon'` — **chaque utilisateur salon devient propriétaire actif de tous les salons de la base**, exécuté automatiquement à la migration (ligne 466). Le backfill correct et déterministe existe séparément en `21_marketplace_legacy_salon_backfill.sql:7-37` mais ne supprime pas les lignes parasites.

### F3 — Trous RLS sur le chemin 04 ⚠️ critique

- `04:404-443` : politiques INSERT/UPDATE `salon_memberships` sans contrainte sur le rôle + aucun REVOKE → n'importe quel utilisateur authentifié peut s'auto-attribuer une membership `owner` via PostgREST, contournant `create_salon_invitation` et son garde manager≠owner (`22:114-116`).
- `04:386-397` et `20:182-191` : UPDATE `professional_profiles` sans restriction de colonnes → un professionnel peut s'auto-`verified`/`published`, contournant tout le pipeline de vérification (Epic 15).

### F4 — Booking : AC 14.3 non satisfaite sur le chemin vivant + double stack ⚠️ majeur

- Deux stacks de réservation complètes coexistent : legacy 04 (`bookings` + `create_booking_transaction`, check-then-insert sans verrou ni contrainte d'exclusion → course TOCTOU) et marketplace 40/73 (`marketplace_bookings` + `create_marketplace_booking`).
- La stack vivante (40/73) **n'a aucune clé d'idempotence** (pas de colonne, pas de paramètre) — AC 14.3 « booking create endpoint requires idempotency key » non implémentée ; `api/marketplace/bookings/route.ts:31` régénère une clé par appel.
- `create_marketplace_booking` écrit `ends_at = starts_at + interval '1 minute'` (`73:139,166`) indépendamment de la durée réelle → la contrainte d'exclusion `marketplace_bookings_no_professional_overlap` (`40:62-67`) **ne protège pas les vrais chevauchements**.

### F5 — Créations service-role sans vérification d'appartenance ⚠️ majeur

- `api/marketplace/jobs` → `careers.ts:39-63` : `createJobPosting` ignore `userId`, insère avec `salonId` client, `status:'active'` immédiat, sans modération — n'importe qui publie des offres au nom de n'importe quel salon. Le chemin correct existe en parallèle (`/api/careers/jobs` : membership owner/manager + draft + modération).
- `api/marketplace/services` → `services-discovery.ts:49-75` : `createBookableService` ignore `userId` aussi — injection de services sur n'importe quel salon/professionnel (AC 13.2 et 16.1 contournées).

### F6 — Vérification : les tests promis par les AC n'existent pas ⚠️ majeur (DoD #2/#9 non tenus)

- AC 14.3 « concurrency integration test » : **absent** (aucun test n'exécute `create_marketplace_booking`).
- AC 14.8 « E2E booking lifecycle » : **absent** — `web/supabase/tests/40_42_marketplace_booking_e2e.sql` est purement structurel (EXISTS sur pg_catalog, 19 lignes) malgré son nom.
- AC 16.5 « e2e application → hired → invite → membership » : **absent** ; pire, `careers.ts:88-107` upsert directement `salon_memberships` sur « hired » avec une colonne `state` inexistante en schéma 20 — exactement ce que l'AC 16.5 interdit.
- DoD #2 « negative authorization tests » : aucun test négatif route-level ; zéro infrastructure de test JS (aucun `*.test.ts`, aucun vitest/jest/playwright).
- Les workflows CI n'exécutent que les contrats 20/21/12 ; les tests 22/23/24 (behavioraux) **ne tournent jamais en CI** ; aucun workflow n'applique la chaîne complète des migrations.

### F7 — Intégrité du processus de validation ⚠️ majeur (DoD #10 non tenu)

- Gates M1–M5 passées `review_pending_ci → passed` **3 minutes après la fusion** (`060c9da` 20:58 vs merge `3e189e0` 20:55), sans aucune preuve de run CI (la revue cross-epic documente « no workflow runs returned »).
- Le build Next était **cassé après le flip** : `d1585fb` (21:15) corrige un paramètre de route qui cassait le build — après que le track fut déclaré « validated ».
- Le garde du checker statique est déjoué par une sous-chaîne : `check_marketplace_v2_contract.py:89-91` exige la présence du texte `review_pending_ci`, qui survit dans la note historique du yaml (ligne 136) alors que les vraies gates sont `passed`.
- `marketplace-v2-review.yml` ne fait ni `npm run build` ni application de migrations.

### F8 — Données de démo/fallback fabriquées ⚠️ majeur

- `api/marketplace/discover/route.ts:89-101` (commit `26fb196`) : sur erreur RPC, renvoie **HTTP 200 avec 3 prestataires fictifs** (notes 4.85–4.95, badge « Vérifié ») — masque toute panne de schéma (très probable vu F1).
- `80_marketplace_demo_seed.sql` : seed `verified`+`published` et **agrégats d'avis fabriqués** (4.80–4.95) en upsert sans gate d'environnement ; insère dans une VIEW (`marketplace_review_aggregates`) → échec d'application garanti ; FK vers des users seed inexistants. Conflit direct avec AC 15.3 « zero/low review counts displayed honestly ».

### F9 — Code mort à risque fail-open ⚠️ modéré

- `entitlements.ts:20-108` : resolver de capacités TS à zéro appelant, lit `salons.plan` sans contrôle d'expiration (contraire au resolver SQL `23:36-151`), colonnes 04 — chemin fail-open dès qu'il sera câblé. `marketplace.ts:159` accepte un plan client.
- `booking-engine.ts:61-99` : `updateBookingStatus` sans aucune autorisation (IDOR latent), event d'audit hors transaction.
- `salon-quota.ts:24-40` : interroge des colonnes inexistantes (`plan_name`) → toujours FREE/5.
- `telemetry.ts:3-18` : helper analytics à zéro appelant écrivant dans la table 04-only, **contournant le scrubbing PII** du route funnel (`70`).

### F10 — 17.5 contournable : seconde table sponsored non gardée ⚠️ modéré

`73:118-145` crée `marketplace_sponsored_listings` sans aucun des gardes que `72:22-41` impose (flag feature, label « Sponsorisé », éligibilité, capability) et l'expose publiquement → le « disabled by default » serveur de l'AC 17.5 est défait par la seconde table.

### F11 — Checkout : compatibilité legacy ⇒ managers révoqués ⚠️ modéré

`api/v1/payments/checkout/route.ts:61-63` : la branche `allowedByLegacyCompatibility` (`user_profiles.salon_id`) ne s'éteint jamais quand une membership est révoquée → un manager parti peut continuer à acheter/renouveler l'abonnement du salon.

### F12 — UX V2 : le middleware force toujours l'onboarding V1 ⚠️ modéré

`web/src/middleware.ts:11,38-44` (non modifié dans la plage) redirige tout utilisateur non configuré vers `/onboarding` V1 (choix binaire customer|salon) alors que l'UX V2 validée — autorité déclarée par le tracker — supprime ce forçage (« ne pas forcer à choisir immédiatement ») ; le flux V2 existe en parallèle sur `/pro/onboarding`.

### F13 — Spécifications périmées (deviations documentées) ℹ️

- Noms de migrations spec (`12/13/15/16/17/18`) ≠ réalité (`20/25/27+28/40-42/50/60`) — renumbering documenté dans les en-têtes (« 20+ range ») mais `story-12-1` (frontmatter ligne 8) et `review-12-1` citent un fichier inexistant ; le numéro 12 est occupé par `12_trellis2_job_checkpoints.sql`.
- Stories 12.7/13.8 ajoutées par l'amendement UX V2 (croissance de périmètre documentée, pas silencieuse).
- 17.4/17.5 couvertes par 3 migrations (71/72/73) dont 73 ré-implémente partiellement 71/72.

### F14 — Endpoint disponibilité non borné (DoS trivial) ⚠️ modéré

`api/marketplace/booking/availability` (non authentifié) transmet `from`/`to` bruts à `list_service_availability` (`40:170-197`) dont le `generate_series` n'a pas de borne — `?from=2000-01-01&to=2100-01-01` ≈ 1,7 M créneaux × 3 sous-requêtes corrélées.

### F15 — Edge cases behavioraux divers ⚠️ modéré

- Chemin professionnel de `create_marketplace_booking` (`73:69-103`) ne vérifie ni time blocks ni règles d'heures → créneaux cachés réservables par API.
- `buffer_before` jamais réservé avant le créneau (`40:91`).
- `completed`/`no_show` possibles avant la date de début (`41:60`).
- Recherche GPS : providers à visibilité « city » sans point invisibles (`51:40`).
- Denylist PII télémétrie incomplète (`api/telemetry/marketplace/route.ts:5-6` — gps/coords/head/biometric passent).
- Lien d'invitation hire → `/invitations/salon` : route inexistante (404, handoff jamais accepté) — `careers/hire-invitation/route.ts:8`.

### F16 — Duplication & dérive de motifs ℹ️→⚠️

- Client Supabase user-scoped recopié ×6 ; `getServiceSupabase` ×2 implémentations ; prédicat d'autorisation membership ré-écrit ×13 routes + ×8 sites SQL malgré `resolve_marketplace_capability`.
- Deux enveloppes d'erreur (anglais `err.message` brut — canal de fuite d'information — vs français structuré).
- Routes jumelles `booking` vs `bookings` ; segment `[salonId]` qui reçoit un slug.
- Deux styles de génération de code (lib documentée multi-lignes vs routes minifiées une-ligne).

## Vérification du comportement

Exécuté le 2026-08-22 (sonde dédiée, base jetable `retro_probe` sur le conteneur `afrofade-db` postgres:16, mock `auth`+`storage` identique aux tests CI, puis base supprimée) :

1. **TypeScript typecheck** (`npm run typecheck` dans `web/`) : **PASS**.
2. **Checker statique** (`scripts/check_marketplace_v2_contract.py`) : **PASS** — mais démonstration faite qu'il est contourné (F7) : il passe alors que les gates sont déjà `passed`.
3. **Chaîne de migrations complète sur base vierge** (ordre lexical, `ON_ERROR_STOP`) : **ÉCHEC à `20_marketplace_identity_foundation.sql:45`** (`column "slug" does not exist`) après 01→12 OK. Reproduction directe de F1. Arrêt à 27 attendu par ailleurs : `CREATE EXTENSION postgis` indisponible sur `postgres:16-alpine` (limite d'environnement, non testable ici).
4. **Base locale vivante** (`afrofade_db`, stack docker up depuis 13 h) : **0 tables** — le schéma marketplace n'a jamais été appliqué sur aucune base réelle de cet environnement ; les fonctionnalités marketplace n'ont jamais tourné bout-en-bout ici.

Périmètre rétréci : aucun parcours applicatif complet (recherche → réservation → notification) n'a été exercé via l'API/UI — l'échec de migration F1 rend ce test impossible sur base vierge, et la base locale vide le rend sans objet ; les constats F4/F5/F6 documentent par ailleurs que ces parcours n'ont jamais été exécutés par des tests.

## Suivi de la rétrospective précédente (Épic 5)

| Action | Statut | Preuve |
| :--- | :--- | :--- |
| `epic-5-retro-item-1-deploy-vps` — Déployer sur le VPS de production (Docker + Nginx SSL) | **open/deferred** | `sprint-status.yaml` action_items : `status: deferred` « Deferred until P0 PR merge + production migrations/secrets validation » ; aucune trace de déploiement dans le repo |
| `epic-5-retro-item-2-e2e-tests` — E2E webhook GeniusPay (Playwright/Cypress) | **open — non réalisé** | Aucun fichier playwright/cypress dans le repo, aucun test webhook (`find`/`grep` sur `web/package.json`, `web/tests`, `web/e2e` : néant) au 2026-08-22 |

Les deux actions de la rétro précédente restent ouvertes ; la seconde (E2E) recoupe le gap structurel F6 (aucune infrastructure de test JS n'a jamais été posée).

## Suivi de la rétrospective précédente (Épic 5)

_À remplir_

## Actions à suivre (Action Items)

Proposées, non appliquées — la rétrospective n'exécute pas la boucle de dev. Constats critiques re-vérifiés à la source avant routage (F1 par reproduction, F2/F5/F11 par relecture).

| # | Priorité | Action | Responsable | Constats |
| :-- | :-- | :--- | :--- | :--- |
| A1 | P0 | Dé-forker le schéma : restaurer `04_role_dashboards.sql` à son contenu pré-piste (`git show d0c738e^:web/supabase/migrations/04_role_dashboards.sql`) ; les migrations 20+ redeviennent l'unique source du schéma marketplace ; re-valider que la chaîne complète s'applique sur base vierge | Dev/Backend | F1, F2, F3 |
| A2 | P0 | CI « fresh-database chain » : job Postgres appliquant `web/supabase/migrations/*.sql` dans l'ordre lexical (mock auth/storage) puis exécutant **tous** les contrats `web/supabase/tests/*.sql` (22/23/24 jamais exécutés) ; ajouter `npm run build` à `marketplace-v2-review.yml` ; remplacer la garde substring du checker statique par un parsing YAML des gates | Dev/Infra | F1, F6, F7 |
| A3 | P0 | Supprimer le backfill CROSS JOIN (`04:444-466`, couvert par A1) et ajouter au contrat 21 un test négatif : aucun utilisateur ne détient d'ownership sur un salon ≠ `user_profiles.salon_id` | Dev/Backend | F2 |
| A4 | P0 | Durcir RLS : `REVOKE INSERT,UPDATE` sur `salon_memberships` (écritures via RPC 22 uniquement) ; verrouiller `verification_status`/`listing_status` de `professional_profiles` (trigger ou RPC whitelist de colonnes) | Dev/Backend | F3 |
| A5 | P0 | Gates d'autorisation sur `api/marketplace/jobs` et `api/marketplace/services` : membership owner/manager active ou propriété du profil pro — déléguer aux chemins `/api/careers/*` qui les implémentent déjà | Dev | F5 |
| A6 | P1 | Booking : colonne `idempotency_key` + paramètre RPC sur `create_marketplace_booking` ; `ends_at` = durée réelle + buffers (au lieu de `starts_at+1min`) | Dev/Backend | F4 |
| A7 | P1 | Purger le code mort fail-open : `entitlements.ts`, `salon-quota.ts`, `telemetry.ts` (helper bypassant le scrubbing PII), `booking-engine.updateBookingStatus`, `careers.updateApplicationStatus` (upsert direct) → chemins RPC existants | Dev | F9, F4 |
| A8 | P1 | Fallback discovery : 503 + `{results:[],degraded:true}` au lieu de `FALLBACK_DISCOVER_RESULTS` ; migration 80 : gate d'environnement, suppression des agrégats d'avis fabriqués et de l'insert dans la view | Dev | F8 |
| A9 | P1 | Clamp de fenêtre sur l'API availability (route + SQL) ; éteindre la branche legacy du checkout en présence d'un historique de membership ; appliquer les gardes de 72 à la table 73 (ou la supprimer) | Dev | F14, F11, F10 |
| A10 | P2 | Aligner le middleware sur l'UX V2 : retirer le forçage `/onboarding` V1 (choix binaire) au profit du parcours intentionnel | Dev/Web | F12 |
| A11 | P2 | Réconcilier la spec : noms de migrations réels dans `epics-marketplace-2026-08-21.md`, frontmatter `story-12-1` et `review-12-1` (citations de fichiers inexistants) | PM | F13 |
| A12 | Process | Écrire les conventions : jamais modifier une migration déjà appliquée ; référencer l'ID de story dans chaque sujet de commit (16/39 seulement) ; un artefact story/review par story (21 stories sans rien pour les épiques 14–17) ; poser l'infrastructure de test JS/E2E (rejoint `epic-5-retro-item-2`) | Dev + PM | F6, F13, F16 |

## Verdict d'acceptation

**`rejected`** — critères **déclarés** (`epics-marketplace-2026-08-21.md` : AC par story + DoD cross-story).

Le verdict machine est **rejeté** pour deux raisons indépendantes et cumulées :

1. **Stories non terminées** : les 39 stories de la piste sont en statut `review`/`review_feature_gated`, aucune en `done` dans `marketplace-sprint-status.yaml` (liste complète en Résumé). L'utilisateur a accepté de procéder à la rétro malgré cette liste (décision enregistrée), ce qui autorise l'analyse mais pas l'acceptation.
2. **Critères déclarés non tenus, constat bloquant non résolu** :
   - AC 14.3 : pas de clé d'idempotence sur le chemin de réservation vivant (migrations 40/73), `ends_at` figé à 1 minute — la contrainte anti-chevauchement ne protège pas les réservations réelles ;
   - AC 14.8 et 16.5 : les tests E2E exigés n'existent pas (le fichier « e2e » 40_42 est purement structurel) ;
   - DoD #2 : aucun test négatif d'autorisation ; DoD #5 : build absent du workflow de review (et le build a cassé après le passage des gates) ; DoD #9 : 21 stories sans artefact de preuve AC ; DoD #10 : gates passées sans preuve CI, 3 minutes après la fusion ;
   - **F1 (bloquant, reproduit)** : la chaîne de migrations casse sur toute base vierge à la migration 20 — un environnement neuf ne peut pas être provisionné.

Une décision humaine peut surclasser ce verdict (règle 1 du barème) ; sans décision contraire, la piste est enregistrée **non acceptée**.

## Questions ouvertes

1. **Quelle forme de schéma est en production réelle ?** La base locale docker est vide (0 tables) et aucun déploiement VPS n'est documenté — si une base Supabase distante existe, déterminer si elle a reçu le 04 édité (tables 04) ou les migrations 20+ : le plan de réparation (A1) en dépend.
2. **Faut-il promouvoir les stories en `done` après remédiation ?** La promotion ne devrait suivre que l'exécution verte du CI de A2 — pas être antérieure, contrairement à ce qui s'est produit pour les gates.
3. **17.4/17.5** : l'activation commerciale reste une décision produit indépendante (CC) ; A9 (gardes sur 73) est un prérequis technique à toute activation.
4. Les décisions D08–D15 et l'amendement UX V2 sont-ils toujours d'autorité pour la suite (l'écart middleware F12 suggère qu'une clarification suffirait) ?
